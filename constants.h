#ifndef CONSTANTS_H
#define CONSTANTS_H

#include <QSize>

static const int MAX_ID_LENGTH = 20;
static const int MAX_PASSWORD_LENGTH = 18;
static const int MIN_PASSWORD_LENGTH = 6;
static const int MAX_NAME_LENGTH = 100;

static const QSize INITIAL_DIALOG_SIZE = QSize(415, 200);

static const int MAX_FILTER_DIALOG_WIDTH = 800;
static const int MAX_FILTER_DIALOG_HEIGHT = 500;

#endif // CONSTANTS_H
